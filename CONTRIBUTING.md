# Contributing

Issues and pull requests are welcome.

- Run `pytest -q` before opening a PR; add a test for every new rule or format.
- **Never attach real customer or utility data** to issues or PRs. Reproduce problems with synthetic data (see `examples/`).
- Keep the scripts free of heavy dependencies; everything must run in a plain Python 3.10+ sandbox.
